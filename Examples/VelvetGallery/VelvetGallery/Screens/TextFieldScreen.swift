import SwiftUI
import DesignSystem

struct TextFieldScreen: View {
    var body: some View {
        GalleryScreen(title: "Text Field", caption: "Inputs & validation") {
            Text("TODO").ds(.body)
        }
    }
}
