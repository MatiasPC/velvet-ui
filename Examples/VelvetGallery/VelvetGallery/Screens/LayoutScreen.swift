import SwiftUI
import DesignSystem

struct LayoutScreen: View {
    var body: some View {
        GalleryScreen(title: "Layout", caption: "Stacks & grids") {
            Text("TODO").ds(.body)
        }
    }
}
