import SwiftUI
import DesignSystem

struct RatingScreen: View {
    var body: some View {
        GalleryScreen(title: "Rating", caption: "Star input") {
            Text("TODO").ds(.body)
        }
    }
}
