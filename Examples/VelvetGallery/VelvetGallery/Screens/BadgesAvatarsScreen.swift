import SwiftUI
import DesignSystem

struct BadgesAvatarsScreen: View {
    var body: some View {
        GalleryScreen(title: "Badges & Avatars", caption: "Status marks & identity") {
            Text("TODO").ds(.body)
        }
    }
}
