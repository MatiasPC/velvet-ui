import SwiftUI
import DesignSystem

struct CodeFieldScreen: View {
    var body: some View {
        GalleryScreen(title: "Code Field", caption: "OTP entry") {
            Text("TODO").ds(.body)
        }
    }
}
