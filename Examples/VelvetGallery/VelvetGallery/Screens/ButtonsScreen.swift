import SwiftUI
import DesignSystem

struct ButtonsScreen: View {
    var body: some View {
        GalleryScreen(title: "Buttons", caption: "Variants, sizes, states") {
            Text("TODO").ds(.body)
        }
    }
}
