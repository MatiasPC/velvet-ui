import SwiftUI
import DesignSystem

struct ToggleScreen: View {
    var body: some View {
        GalleryScreen(title: "Toggle", caption: "Switches") {
            Text("TODO").ds(.body)
        }
    }
}
