import SwiftUI
import DesignSystem

struct PageControlScreen: View {
    var body: some View {
        GalleryScreen(title: "Page Control", caption: "Paged content") {
            Text("TODO").ds(.body)
        }
    }
}
