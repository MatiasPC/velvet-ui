import SwiftUI
import DesignSystem

struct ThemeScreen: View {
    var body: some View {
        GalleryScreen(title: "Theme", caption: "Neutral palette & glass surfaces") {
            Text("TODO").ds(.body)
        }
    }
}
