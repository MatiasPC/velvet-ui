import SwiftUI
import DesignSystem

struct SegmentedControlScreen: View {
    var body: some View {
        GalleryScreen(title: "Segmented Control", caption: "Pick one") {
            Text("TODO").ds(.body)
        }
    }
}
