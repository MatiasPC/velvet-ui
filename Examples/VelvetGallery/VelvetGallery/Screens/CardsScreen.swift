import SwiftUI
import DesignSystem

struct CardsScreen: View {
    var body: some View {
        GalleryScreen(title: "Cards", caption: "Glass containers") {
            Text("TODO").ds(.body)
        }
    }
}
